version: "3.3"
services:
  api:
    image: "phiphi"
    build:
      context: ../../
      args:
        PROJECT: phiphi
    command: ["uvicorn", "phiphi.main:app", "--host", "0.0.0.0", "--port", "80"]
    ports:
      - "8080:80"
